Temporary Firestore rules for this classroom activity.

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /onlineSafetyCaseStudyResponses/{docId} {
      allow create: if true;
      allow read: if true;
      allow update, delete: if false;
    }
  }
}

Important:
These are open classroom activity rules. After the lesson, change read/create to false.
